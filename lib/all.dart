import 'package:flutter/material.dart';

class All extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
        return Scaffold(
            appBar: AppBar(
                title: Text('All'),
            ), // AppBar
            body: Center(
                child: Text('Welcome to All'),
            ), // Center
        ); // Scaffold
    }
}
